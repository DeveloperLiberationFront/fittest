#ABCI one sentence summary

= abci =

|| src || doc || download ||

Abci is an instrumentation tool to inject low-level logging code into a target application. The application is in ActionScript bytecode, packaged as a Flash .swf file.